<div class="flex items-center justify-between flex-col pt-20 pb-[120px]">
  <div class="max-w-2xl px-8 py-4 bg-white rounded-lg shadow-md dark:bg-gray-800">

    <h2 class="text-xl font-bold text-gray-700 dark:text-white mb-4">
      📝 買い物リストを編集
    </h2>

    <%= form_with model: @shopping_list, local: true do |f| %>
      <div class="mb-4">
        <%= f.label :title, "タイトル", class: "block text-gray-700 dark:text-gray-200 mb-1" %>
        <%= f.text_field :title, class: "w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring focus:ring-blue-300 dark:bg-gray-700 dark:text-white dark:border-gray-600" %>
      </div>

      <div class="mb-4">
        <h3 class="text-lg font-semibold text-gray-700 dark:text-white mb-2">🛒 商品リスト</h3>
        <%= f.fields_for :shopping_list_items do |item_fields| %>
          <div class="mb-2">
            <%= item_fields.label :item_id, "商品", class: "block text-sm text-gray-600 dark:text-gray-300" %>
            <%= item_fields.collection_select :item_id, Item.all, :id, :name, { prompt: "選択してください" }, class: "w-full px-3 py-2 border rounded-lg mb-2 dark:bg-gray-700 dark:text-white dark:border-gray-600" %>

            <%= item_fields.label :quantity, "数量", class: "block text-sm text-gray-600 dark:text-gray-300" %>
            <%= item_fields.number_field :quantity, class: "w-24 px-3 py-2 border rounded-lg dark:bg-gray-700 dark:text-white dark:border-gray-600" %>
            <%= link_to '商品を削除', '#', class: "text-black bg-blue-500 rounded-lg"%>
          </div>
        <% end %>
      </div>

      <div class="flex items-center mt-10 gap-4">
        <%= f.submit '更新する', class: "px-6 py-2 font-medium tracking-wide text-white capitalize transition-colors duration-300 transform bg-blue-600 rounded-lg hover:bg-blue-500 focus:outline-none focus:ring focus:ring-blue-300 focus:ring-opacity-80" %>

        <%= link_to 'キャンセル', shopping_lists_path,
            class: "px-6 py-2 font-medium tracking-wide text-white capitalize transition-colors duration-300 transform bg-gray-600 rounded-lg hover:bg-gray-500 focus:outline-none focus:ring focus:ring-gray-300 focus:ring-opacity-80" %>
      </div>
    <% end %>

  </div>
</div>
